# СХЕМА БАЗЫ ДАННЫХ
## Система управления задачами (TaskFlow)

---

## 1. ОБЗОР

**СУБД:** PostgreSQL 14+

**Основные таблицы:**
- `users` — пользователи системы
- `projects` — проекты
- `project_members` — члены проектов
- `tasks` — задачи
- `task_comments` — комментарии к задачам
- `task_history` — история изменений задач
- `notifications` — уведомления
- `sessions` — сессии пользователей

---

## 2. ТАБЛИЦА USERS

**Описание:** Хранит информацию о пользователях системы

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  first_name VARCHAR(100) NOT NULL,
  last_name VARCHAR(100) NOT NULL,
  avatar_url VARCHAR(500),
  bio TEXT,
  timezone VARCHAR(50) DEFAULT 'UTC',
  email_verified BOOLEAN DEFAULT FALSE,
  email_verified_at TIMESTAMP,
  is_active BOOLEAN DEFAULT TRUE,
  last_login_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

-- Индексы
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_created_at ON users(created_at);
CREATE INDEX idx_users_is_active ON users(is_active);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `email` — email адрес (уникальный)
- `password_hash` — хеш пароля (bcrypt)
- `first_name` — имя
- `last_name` — фамилия
- `avatar_url` — URL аватара
- `bio` — биография
- `timezone` — часовой пояс
- `email_verified` — подтвержден ли email
- `email_verified_at` — дата подтверждения email
- `is_active` — активен ли пользователь
- `last_login_at` — дата последнего входа
- `created_at` — дата создания
- `updated_at` — дата последнего обновления
- `deleted_at` — дата удаления (soft delete)

---

## 3. ТАБЛИЦА PROJECTS

**Описание:** Хранит информацию о проектах

```sql
CREATE TABLE projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  color VARCHAR(7) DEFAULT '#3498db',
  is_public BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

-- Индексы
CREATE INDEX idx_projects_owner_id ON projects(owner_id);
CREATE INDEX idx_projects_created_at ON projects(created_at);
CREATE INDEX idx_projects_is_public ON projects(is_public);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `owner_id` — ID владельца проекта (FK на users)
- `name` — название проекта
- `description` — описание проекта
- `color` — цвет проекта (hex)
- `is_public` — публичный ли проект
- `created_at` — дата создания
- `updated_at` — дата последнего обновления
- `deleted_at` — дата удаления (soft delete)

---

## 4. ТАБЛИЦА PROJECT_MEMBERS

**Описание:** Хранит информацию о членах проектов

```sql
CREATE TABLE project_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role VARCHAR(50) DEFAULT 'member', -- admin, member
  status VARCHAR(50) DEFAULT 'active', -- active, pending, inactive
  invited_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  joined_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(project_id, user_id)
);

-- Индексы
CREATE INDEX idx_project_members_project_id ON project_members(project_id);
CREATE INDEX idx_project_members_user_id ON project_members(user_id);
CREATE INDEX idx_project_members_role ON project_members(role);
CREATE INDEX idx_project_members_status ON project_members(status);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `project_id` — ID проекта (FK на projects)
- `user_id` — ID пользователя (FK на users)
- `role` — роль в проекте (admin, member)
- `status` — статус (active, pending, inactive)
- `invited_at` — дата приглашения
- `joined_at` — дата присоединения
- `created_at` — дата создания
- `updated_at` — дата последнего обновления

---

## 5. ТАБЛИЦА TASKS

**Описание:** Хранит информацию о задачах

```sql
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  created_by UUID NOT NULL REFERENCES users(id) ON DELETE SET NULL,
  assigned_to UUID REFERENCES users(id) ON DELETE SET NULL,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  priority VARCHAR(50) DEFAULT 'medium', -- low, medium, high
  status VARCHAR(50) DEFAULT 'todo', -- todo, in_progress, done
  due_date TIMESTAMP,
  completed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

-- Индексы
CREATE INDEX idx_tasks_project_id ON tasks(project_id);
CREATE INDEX idx_tasks_created_by ON tasks(created_by);
CREATE INDEX idx_tasks_assigned_to ON tasks(assigned_to);
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_priority ON tasks(priority);
CREATE INDEX idx_tasks_due_date ON tasks(due_date);
CREATE INDEX idx_tasks_created_at ON tasks(created_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `project_id` — ID проекта (FK на projects)
- `created_by` — ID создателя (FK на users)
- `assigned_to` — ID назначенного пользователя (FK на users)
- `title` — название задачи
- `description` — описание задачи
- `priority` — приоритет (low, medium, high)
- `status` — статус (todo, in_progress, done)
- `due_date` — срок выполнения
- `completed_at` — дата выполнения
- `created_at` — дата создания
- `updated_at` — дата последнего обновления
- `deleted_at` — дата удаления (soft delete)

---

## 6. ТАБЛИЦА TASK_TAGS

**Описание:** Хранит теги для задач

```sql
CREATE TABLE task_tags (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
  tag VARCHAR(100) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(task_id, tag)
);

-- Индексы
CREATE INDEX idx_task_tags_task_id ON task_tags(task_id);
CREATE INDEX idx_task_tags_tag ON task_tags(tag);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `task_id` — ID задачи (FK на tasks)
- `tag` — название тега
- `created_at` — дата создания

---

## 7. ТАБЛИЦА TASK_COMMENTS

**Описание:** Хранит комментарии к задачам

```sql
CREATE TABLE task_comments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
  author_id UUID NOT NULL REFERENCES users(id) ON DELETE SET NULL,
  content TEXT NOT NULL,
  edited BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

-- Индексы
CREATE INDEX idx_task_comments_task_id ON task_comments(task_id);
CREATE INDEX idx_task_comments_author_id ON task_comments(author_id);
CREATE INDEX idx_task_comments_created_at ON task_comments(created_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `task_id` — ID задачи (FK на tasks)
- `author_id` — ID автора (FK на users)
- `content` — содержание комментария
- `edited` — был ли отредактирован
- `created_at` — дата создания
- `updated_at` — дата последнего обновления
- `deleted_at` — дата удаления (soft delete)

---

## 8. ТАБЛИЦА TASK_MENTIONS

**Описание:** Хранит упоминания пользователей в комментариях

```sql
CREATE TABLE task_mentions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  comment_id UUID NOT NULL REFERENCES task_comments(id) ON DELETE CASCADE,
  mentioned_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(comment_id, mentioned_user_id)
);

-- Индексы
CREATE INDEX idx_task_mentions_comment_id ON task_mentions(comment_id);
CREATE INDEX idx_task_mentions_mentioned_user_id ON task_mentions(mentioned_user_id);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `comment_id` — ID комментария (FK на task_comments)
- `mentioned_user_id` — ID упомянутого пользователя (FK на users)
- `created_at` — дата создания

---

## 9. ТАБЛИЦА TASK_HISTORY

**Описание:** Хранит историю изменений задач

```sql
CREATE TABLE task_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
  changed_by UUID NOT NULL REFERENCES users(id) ON DELETE SET NULL,
  action VARCHAR(100) NOT NULL, -- created, updated, status_changed, assigned, completed
  field_name VARCHAR(100),
  old_value TEXT,
  new_value TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_task_history_task_id ON task_history(task_id);
CREATE INDEX idx_task_history_changed_by ON task_history(changed_by);
CREATE INDEX idx_task_history_action ON task_history(action);
CREATE INDEX idx_task_history_created_at ON task_history(created_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `task_id` — ID задачи (FK на tasks)
- `changed_by` — ID пользователя, который сделал изменение (FK на users)
- `action` — тип действия (created, updated, status_changed, assigned, completed)
- `field_name` — название измененного поля
- `old_value` — старое значение
- `new_value` — новое значение
- `created_at` — дата создания

---

## 10. ТАБЛИЦА NOTIFICATIONS

**Описание:** Хранит уведомления пользователей

```sql
CREATE TABLE notifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  type VARCHAR(100) NOT NULL, -- task_assigned, task_completed, comment_added, mention, due_date_reminder
  title VARCHAR(255) NOT NULL,
  message TEXT NOT NULL,
  related_id UUID, -- ID связанного ресурса (task, comment, etc.)
  read BOOLEAN DEFAULT FALSE,
  read_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

-- Индексы
CREATE INDEX idx_notifications_user_id ON notifications(user_id);
CREATE INDEX idx_notifications_type ON notifications(type);
CREATE INDEX idx_notifications_read ON notifications(read);
CREATE INDEX idx_notifications_created_at ON notifications(created_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `type` — тип уведомления
- `title` — заголовок уведомления
- `message` — текст уведомления
- `related_id` — ID связанного ресурса
- `read` — прочитано ли уведомление
- `read_at` — дата прочтения
- `created_at` — дата создания
- `deleted_at` — дата удаления (soft delete)

---

## 11. ТАБЛИЦА NOTIFICATION_PREFERENCES

**Описание:** Хранит предпочтения уведомлений пользователей

```sql
CREATE TABLE notification_preferences (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE,
  email_notifications BOOLEAN DEFAULT TRUE,
  due_date_reminders BOOLEAN DEFAULT TRUE,
  assignment_notifications BOOLEAN DEFAULT TRUE,
  comment_notifications BOOLEAN DEFAULT TRUE,
  mention_notifications BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_notification_preferences_user_id ON notification_preferences(user_id);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `email_notifications` — включены ли email уведомления
- `due_date_reminders` — включены ли напоминания о сроках
- `assignment_notifications` — включены ли уведомления о назначении
- `comment_notifications` — включены ли уведомления о комментариях
- `mention_notifications` — включены ли уведомления об упоминаниях
- `created_at` — дата создания
- `updated_at` — дата последнего обновления

---

## 12. ТАБЛИЦА SESSIONS

**Описание:** Хранит сессии пользователей

```sql
CREATE TABLE sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash VARCHAR(255) NOT NULL UNIQUE,
  refresh_token_hash VARCHAR(255) UNIQUE,
  ip_address VARCHAR(45),
  user_agent TEXT,
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_sessions_user_id ON sessions(user_id);
CREATE INDEX idx_sessions_token_hash ON sessions(token_hash);
CREATE INDEX idx_sessions_expires_at ON sessions(expires_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `token_hash` — хеш JWT токена
- `refresh_token_hash` — хеш refresh токена
- `ip_address` — IP адрес
- `user_agent` — User Agent браузера
- `expires_at` — дата истечения сессии
- `created_at` — дата создания
- `updated_at` — дата последнего обновления

---

## 13. ТАБЛИЦА EMAIL_VERIFICATIONS

**Описание:** Хранит токены для подтверждения email

```sql
CREATE TABLE email_verifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash VARCHAR(255) NOT NULL UNIQUE,
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_email_verifications_user_id ON email_verifications(user_id);
CREATE INDEX idx_email_verifications_token_hash ON email_verifications(token_hash);
CREATE INDEX idx_email_verifications_expires_at ON email_verifications(expires_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `token_hash` — хеш токена подтверждения
- `expires_at` — дата истечения токена
- `created_at` — дата создания

---

## 14. ТАБЛИЦА PASSWORD_RESETS

**Описание:** Хранит токены для сброса пароля

```sql
CREATE TABLE password_resets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash VARCHAR(255) NOT NULL UNIQUE,
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_password_resets_user_id ON password_resets(user_id);
CREATE INDEX idx_password_resets_token_hash ON password_resets(token_hash);
CREATE INDEX idx_password_resets_expires_at ON password_resets(expires_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `token_hash` — хеш токена сброса
- `expires_at` — дата истечения токена
- `created_at` — дата создания

---

## 15. ТАБЛИЦА AUDIT_LOGS

**Описание:** Хранит логи всех действий в системе

```sql
CREATE TABLE audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  action VARCHAR(100) NOT NULL,
  resource_type VARCHAR(100) NOT NULL,
  resource_id UUID,
  details JSONB,
  ip_address VARCHAR(45),
  user_agent TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX idx_audit_logs_user_id ON audit_logs(user_id);
CREATE INDEX idx_audit_logs_action ON audit_logs(action);
CREATE INDEX idx_audit_logs_resource_type ON audit_logs(resource_type);
CREATE INDEX idx_audit_logs_created_at ON audit_logs(created_at);
```

**Поля:**
- `id` — уникальный идентификатор (UUID)
- `user_id` — ID пользователя (FK на users)
- `action` — тип действия
- `resource_type` — тип ресурса
- `resource_id` — ID ресурса
- `details` — дополнительные детали (JSON)
- `ip_address` — IP адрес
- `user_agent` — User Agent браузера
- `created_at` — дата создания

---

## 16. ДИАГРАММА СВЯЗЕЙ

```
┌─────────────────┐
│     users       │
├─────────────────┤
│ id (PK)         │
│ email           │
│ password_hash   │
│ first_name      │
│ last_name       │
│ avatar_url      │
│ timezone        │
│ created_at      │
└────────┬────────┘
         │
         ├─────────────────────────────────────┐
         │                                     │
         │ 1:N                            1:N  │
         │                                     │
    ┌────▼──────────────┐          ┌──────────▼────────┐
    │    projects       │          │ project_members   │
    ├───────────────────┤          ├───────────────────┤
    │ id (PK)           │          │ id (PK)           │
    │ owner_id (FK)     │          │ project_id (FK)   │
    │ name              │          │ user_id (FK)      │
    │ description       │          │ role              │
    │ color             │          │ status            │
    │ is_public         │          └───────────────────┘
    │ created_at        │
    └────┬──────────────┘
         │
         │ 1:N
         │
    ┌────▼──────────────┐
    │     tasks         │
    ├───────────────────┤
    │ id (PK)           │
    │ project_id (FK)   │
    │ created_by (FK)   │
    │ assigned_to (FK)  │
    │ title             │
    │ description       │
    │ priority          │
    │ status            │
    │ due_date          │
    │ completed_at      │
    │ created_at        │
    └────┬──────────────┘
         │
         ├──────────────────────────────┐
         │                              │
         │ 1:N                     1:N  │
         │                              │
    ┌────▼──────────────┐    ┌─────────▼────────┐
    │ task_comments     │    │  task_tags       │
    ├───────────────────┤    ├──────────────────┤
    │ id (PK)           │    │ id (PK)          │
    │ task_id (FK)      │    │ task_id (FK)     │
    │ author_id (FK)    │    │ tag              │
    │ content           │    └──────────────────┘
    │ edited            │
    │ created_at        │
    └───────────────────┘
         │
         │ 1:N
         │
    ┌────▼──────────────┐
    │ task_mentions     │
    ├───────────────────┤
    │ id (PK)           │
    │ comment_id (FK)   │
    │ mentioned_user_id │
    └───────────────────┘
```

---

## 17. МИГРАЦИИ

### 17.1 Инициализация БД

```sql
-- Создание расширений
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Создание всех таблиц (см. выше)
```

### 17.2 Добавление триггеров для updated_at

```sql
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = CURRENT_TIMESTAMP;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Применить триггер ко всем таблицам с updated_at
CREATE TRIGGER update_users_updated_at BEFORE UPDATE ON users
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_projects_updated_at BEFORE UPDATE ON projects
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_tasks_updated_at BEFORE UPDATE ON tasks
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_task_comments_updated_at BEFORE UPDATE ON task_comments
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_notification_preferences_updated_at BEFORE UPDATE ON notification_preferences
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_sessions_updated_at BEFORE UPDATE ON sessions
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

---

## 18. ОПТИМИЗАЦИЯ

### 18.1 Рекомендуемые индексы для часто используемых запросов

```sql
-- Получить все задачи пользователя
CREATE INDEX idx_tasks_assigned_to_status ON tasks(assigned_to, status);

-- Получить все проекты пользователя
CREATE INDEX idx_projects_owner_id_created_at ON projects(owner_id, created_at DESC);

-- Получить все члены проекта
CREATE INDEX idx_project_members_project_id_status ON project_members(project_id, status);

-- Получить все комментарии задачи
CREATE INDEX idx_task_comments_task_id_created_at ON task_comments(task_id, created_at DESC);

-- Получить все уведомления пользователя
CREATE INDEX idx_notifications_user_id_read_created_at ON notifications(user_id, read, created_at DESC);
```

### 18.2 Партиционирование (для больших таблиц)

```sql
-- Партиционирование таблицы task_history по дате
CREATE TABLE task_history_2026_q1 PARTITION OF task_history
  FOR VALUES FROM ('2026-01-01') TO ('2026-04-01');

CREATE TABLE task_history_2026_q2 PARTITION OF task_history
  FOR VALUES FROM ('2026-04-01') TO ('2026-07-01');

-- И так далее...
```

---

## 19. РЕЗЕРВНОЕ КОПИРОВАНИЕ

### 19.1 Скрипт резервного копирования

```bash
#!/bin/bash

# Ежедневное резервное копирование
pg_dump -U taskflow_user -h localhost taskflow_db | \
  gzip > /backups/taskflow_$(date +%Y%m%d_%H%M%S).sql.gz

# Удалить резервные копии старше 30 дней
find /backups -name "taskflow_*.sql.gz" -mtime +30 -delete
```

### 19.2 Восстановление из резервной копии

```bash
#!/bin/bash

# Восстановление из резервной копии
gunzip < /backups/taskflow_20260703_100000.sql.gz | \
  psql -U taskflow_user -h localhost taskflow_db
```

---

## 20. БЕЗОПАСНОСТЬ

### 20.1 Управление доступом

```sql
-- Создать пользователя БД для приложения
CREATE USER taskflow_app WITH PASSWORD 'secure_password';

-- Выдать необходимые права
GRANT CONNECT ON DATABASE taskflow_db TO taskflow_app;
GRANT USAGE ON SCHEMA public TO taskflow_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO taskflow_app;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO taskflow_app;
```

### 20.2 Шифрование чувствительных данных

```sql
-- Использовать pgcrypto для шифрования паролей
-- Пароли должны быть хеширован на уровне приложения (bcrypt)

-- Для дополнительной безопасности можно использовать:
-- - Шифрование на уровне БД (pgcrypto)
-- - SSL соединение к БД
-- - Аудит логирование всех операций
```